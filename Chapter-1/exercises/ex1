#include <stdio.h>

int main() {
	float fahr, celsium;
	int lower,upper, step;
	
	lower =  0;
	upper = 300;
	step = 20;
	fahr = lower;
	printf("celsium\t fahr\n");
	while (fahr <= upper) {
		celsium = (5.0/9.0) * (fahr - 32.0);
		printf("%3.0f %6.1f\n", fahr,celsium);
		fahr = fahr + step;
	}
}
